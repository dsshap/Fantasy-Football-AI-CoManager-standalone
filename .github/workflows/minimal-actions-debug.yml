name: Minimal Actions Debug

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *' # re-arm after first manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo ping
